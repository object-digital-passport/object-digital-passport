# Object Digital Passport · v0.2

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![GitHub stars](https://img.shields.io/github/stars/object-digital-passport/object-digital-passport?style=flat&logo=github)](https://github.com/object-digital-passport/object-digital-passport/stargazers)

## ВАЖНО: контракты 0.X не предназначены для долгосрочного хранения

В этом репозитории **0.X** — это стадия proof-of-concept. Правила контракта ещё могут меняться.

Простыми словами:
- **Деплоймент** — это один конкретный адрес контракта (один реестр).
- Ваш `creatorId` и паспорта привязаны к этому деплойменту.
- В новом деплойменте 0.X даже тот же кошелёк может получить **другой** `creatorId`.
- Старые данные остаются в старом деплойменте и читаются, но автоматически не переносятся.
- **С v0.1 обратной совместимости нет** ни для ID профиля (`creatorId`), ни для паспортных записей: v0.1 и v0.2 — это разные реестры.

Если вам нужен сценарий **«один кошелёк + один постоянный `creatorId`»** как каноничное долгосрочное хранилище, дождитесь стабильного **v1**.

Короткий пример:
1. Вы зарегистрировались и минтачили в деплойменте A (`contractAddress_A`) и получили `creatorId_A`.
2. Позже выходит деплоймент B (`contractAddress_B`) с обновлёнными правилами.
3. Данные в A остаются читаемыми, но в B может понадобиться новая регистрация и вы можете получить другой `creatorId_B`.

## Термины (прочитайте в первую очередь)

Блокчейн-приложения часто переиспользуют слова из NFT и финансов — вот что они означают **в ODP**:

| Термин | Значение в ODP |
|:--|:--|
| **Минт / minting** | Отправка **транзакции**, которая **создаёт** новую on-chain запись. Для паспорта **mint** означает, что смарт-контракт **регистрирует** ваш объект: он назначает **Passport ID** (поле `humanId`) и хранит криптографические **хэши** (в v0.2: **только gas сети** — без отдельной протокольной комиссии). Вы всё равно **скачиваете** и можете **хостить** `passport.json` отдельно. |
| **Register (`registerCreator`)** | **Одноразовый** on-chain шаг: ваш кошелёк платит **gas** и получает постоянный **ID профиля** (`C-…`, `B-…` или `P-…`; on-chain `creatorId`). Делать это нужно **до** того, как вы сможете минтачить паспорта. |
| **Deployment (деплоймент)** | Один конкретный экземпляр смарт-контракта по одному адресу (один реестр). Ваш `creatorId` и записи привязаны к нему. |
| **Passport** | **Вся** запись по одному объекту: on-chain строка плюс (если опубликован) файл **passport.json** по адресу `dataUrl`. |
| **`passport.json`** | Оффчейн JSON-документ с названием, seal, хэшами и т.д. На on-chain хранится только **хэш**; **вы** должны хранить файл. Если вы указываете `dataUrl`, публикуйте файл там, чтобы верификаторы могли его скачать. |
| **`dataUrl`** | Необязательная **HTTPS** ссылка, где размещён `passport.json`. Если поле пустое on-chain, публичная страница Verify **не сможет** скачать JSON — тогда детали и аутентичность сможет проверить только тот, у кого есть сам **файл**. |
| **Проверка / verification** | **Только чтение**: загружаются данные из сети + (если доступно) `passport.json`, затем пересчитываются хэши — **без** кошелька и без протокольных комиссий. |
| **Gas** | **POL** платится в сеть Polygon за выполнение транзакций (величина зависит от загруженности). В v0.2 нет дополнительного сжигаемого протокольного fee на register/mint — только gas. |
| **Протокольная комиссия** | Деплойменты v0.1 использовали фиксированное сжигание **0.001 POL** на некоторых действиях. В v0.2 этого нет — вы платите **только gas** (POL). |
| **Wallet** | Ethereum-совместимое приложение, которое держит ваши ключи и **подписывает** транзакции. На десктопе референсные HTML-страницы используют **injected** провайдеры (см. ниже “Кошельки (в браузере)”), а не только MetaMask. |

Нормативные определения и форматы: **[`SPEC.md`](SPEC.md)** (особенно §1.1 и дальше по тексту).

### Кошельки (в браузере)

Страницы **Creator** и **Passport** используют `window.ethereum` ([**EIP-1193**](https://eips.ethereum.org/EIPS/eip-1193)): любое браузерное расширение, которое инжектит этот провайдер, может подключаться и подписывать. Примеры: **MetaMask**, **Rabby**, **Coinbase Wallet**, **Brave Wallet**, **OKX Wallet** и другие EIP-1193-совместимые расширения. Хелпер в [`web/creator.html`](web/creator.html) / [`web/passport.html`](web/passport.html) (`injectedEthereum`) предпочитает MetaMask, если доступны `providers[]`; иначе используется первый инжектнутый провайдер.

**Не входит в рамки этого репозитория:** сценарии вроде **WalletConnect** (или аналоги), которые не инжектят `window.ethereum`, в статических страницах не подключены — для них нужна явная интеграция. На **mobile Safari** без расширения используйте “in-app browser” вашего кошелька или открывайте страницы по ссылке из приложения кошелька, чтобы появился тот же injected provider.

### Версионирование (сайт vs контракт)

| | |
|:--|:--|
| **Сайт / статический релиз** | **`0.X.Y`** (см. `ODP_SITE_VERSION` в [`web/odp-contract.js`](web/odp-contract.js)). Увеличивайте `Y`, когда меняете только документацию, HTML/CSS или инструменты **без** нового деплоймента контракта. |
| **Контракт / генерация протокола** | **`0.X`** в спецификациях и git-тегах — когда меняется байткод или правила on-chain (например, v0.1 → v0.2). Референсный контракт отдаёт **`SPEC_MAJOR`** / **`SPEC_MINOR`** (читаемая строка спецификации) и упакованный байт **`CONTRACT_VERSION`** для UI. |
| **On-chain `CONTRACT_VERSION`** | Это **не** маркетинговый semver. UI читает его, чтобы выбрать ABI: **≥2** = spec **v0.2** (gas-only, опциональный `dataUrl`, mint с folder-base, **внешний хэш документа**, префикс **`M` (museum)**, неограниченные **P/M** мints, **`submitProof`** из P или M). **`CONTRACT_VERSION == 0`** (исторические деплойменты v0.1 fee-line) **отклоняются**. См. [`web/odp-contract.js`](web/odp-contract.js). |
| **Упакованный байт** | **`SPEC_MAJOR`/`SPEC_MINOR`** каждая &lt; 16. Spec 0.2 → `SPEC_MINOR=2` → **`CONTRACT_VERSION = 2`**. |
| **Старый mainnet деплой** | Первый публичный Polygon-контракт использовал on-chain байт **0** (legacy fee-line). Текущие HTML из этого репозитория **не поддерживают этот адрес** как `NET.contract`; используйте **v0.2+** деплой. Историческое обсуждение: **`SECURITY.md`**, **`SPEC.md`**. |

### Подпись стека и доверие (всегда включено в веб-интерфейсе)

Страницы в `web/` показывают **панель стека** (см. `odpFormatStackBlockHtml` в [`web/odp-contract.js`](web/odp-contract.js)), чтобы операторы не полагались на скрытые предположения:

| Тема | Что это значит |
|:--|:--|
| **Чтение старых данных** | **read ABI** продолжает декодировать предыдущие on-chain значения **`contractVersion`**. Верификатор сначала вызывает **основной** `NET.contract`; если паспорт или creator **не найден** там, он пробует **`NET.previousContracts`** (старые Polygon-деплойменты — каждый это свой реестр). |
| **SemVer сайта — красный** | **`0.x.y`** (`ODP_SITE_VERSION`) = **красный флаг**: proof-of-concept / не production-stable. |
| **SemVer сайта — зелёный** | **`1.0.0`** и выше: стабильная линия. UI становится **зелёным**, когда major этой страницы совпадает с `ODP_LATEST_STABLE_MAJOR` (поднимать константу нужно при выпуске нового стабильного major, например `2` после `1.x`). |
| **SemVer сайта — жёлтый** | Если latest stable major это **N ≥ 2**, то majors **`1 … N−1`** — **жёлтые** (старые stable-линии: нужна проверка миграции). Major страницы, новее `ODP_LATEST_STABLE_MAJOR`, тоже **жёлтый**, пока не совпадёт константа. |

**Канонические live URL** (GitHub Pages проектный сайт): базовый URL — [https://object-digital-passport.github.io/object-digital-passport/](https://object-digital-passport.github.io/object-digital-passport/) — например [Профиль](https://object-digital-passport.github.io/object-digital-passport/creator.html), [Passport](https://object-digital-passport.github.io/object-digital-passport/passport.html), [Verify](https://object-digital-passport.github.io/object-digital-passport/verify.html).

---

## Что это за система (и зачем она)

**Object Digital Passport (ODP)** — открытый стандарт регистрации **физического или цифрового объекта** в публичном блокчейне и последующего **доказывания** того, что данный объект соответствует регистрации — без проприетарной платформы, подписки или центрального gatekeeper.

Как части “склеиваются”:

- **Passport ID** (`ODP-YYYY-MM-NNNNNNNNN`; в JSON/ABI — `humanId`) — удобный, читаемый и уникальный идентификатор объекта; назначается контрактом, неизменяем.
- **ID профиля** (`C-482-930-174-005` и т.п.; on-chain `creatorId`) — постоянная идентичность художника, бренда или института; нужна перед mint’ом.
- **On-chain record** — компактно: хэши, привязка к creator, URL, metadata seal. **Без** больших картинок и полного JSON на on-chain.
- **Passport JSON** — полный документ, который должен жить по **`dataUrl`**, когда вам нужна публичная веб-верификация (HTTPS); в сети хранится хэш, чтобы любое изменение файла было обнаружено. **`dataUrl` можно не указывать** при mint (v0.2), но тогда подтверждение будет доступно только тем, у кого есть **файл**.

**Ключевые принципы** (из **`SPEC.md` §1**; там нормативные формулировки):

| Принцип | Значение |
|:--|:--|
| **Open** | Любой может реализовать протокол на любом языке/платформе. |
| **Decentralized** | Нет одной компании, которая контролирует реестр. |
| **Offline-friendly** | Подлинность можно проверить без интернета, используя только хэши (см. **SPEC.md**). Типичные веб-верификаторы также читают цепочку и `dataUrl` для полного чека. |
| **Free to read** | Проверка никогда не стоит ничего (чтение в сети использует публичные RPC). |
| **Minimal on-chain** | На on-chain нет изображений и больших данных — только то, что требует **`SPEC.md`**. |

**Этот репозиторий** — **один референсный пример**: Solidity-контракт, статические страницы в `web/`, скрипты деплоя и хелперы — **автор: Andrei Chernikov**. Он демонстрирует идею end-to-end. **Нормативные правила** (форматы ID, хэширование, верификация, seals) находятся в **[`SPEC.md`](SPEC.md)**; обсуждение безопасности — в **[`SECURITY.md`](SECURITY.md)**.

---

## Живая демо-версия (пример UI)

Ниже — **пример** фронтендов. Установите **`NET.contract`** в `web/creator.html`, `web/passport.html` и `web/verify.html` на ваш **v0.2+** деплоймент; UI проверяет **`CONTRACT_VERSION()`** и отвергает legacy байт **0** (см. выше “Versioning”). Поведение протокола задаётся **`SPEC.md`**, а не только этим HTML.

**GitHub Pages:** базовый проект — [https://object-digital-passport.github.io/object-digital-passport/](https://object-digital-passport.github.io/object-digital-passport/) 

| Страница | URL | Примечания |
|:--|:--|:--|
| **Verify** | [verify.html](https://object-digital-passport.github.io/object-digital-passport/verify.html) | Read-only — **без кошелька** |
| **Профиль** | [creator.html](https://object-digital-passport.github.io/object-digital-passport/creator.html) | Нужен кошелёк + gas |
| **Passport** | [passport.html](https://object-digital-passport.github.io/object-digital-passport/passport.html) | Нужен кошелёк + gas |

*Если корень сайта показывает README-стилевый контент, но `verify.html` отдаёт 404, возможно GitHub Pages всё ещё обслуживает сборку отдельной ветки/Jekyll. Предпочитайте Settings → Pages → Source: GitHub Actions и зеленую успешную сборку Deploy GitHub Pages — смотрите [`.github/workflows/pages.yml`](.github/workflows/pages.yml).*

**GitHub Pages — адрес реестра:** статический HTML в git содержит `NET.contract: ""`. Workflow Pages подставляет ваш Polygon address на этапе сборки из **`ODP_CONTRACT_ADDRESS`** (repository secret или variable — строка checksum `0x…`; workflow использует secret, если он задан, иначе variable). Добавьте это в Settings → Secrets and variables → Actions (одинаковое значение для всех трёх страниц). Workflow также пишет `registry-config.json` рядом с HTML; UI грузит его с `cache: no-store`, чтобы после деплоя первая загрузка HTML подтянула адрес даже если браузер закэшировал старый HTML без `NET.contract`. Если secret отсутствует — задайте адрес локально в `web/creator.html`, `web/passport.html` и `web/verify.html` или добавьте `web/registry-config.json` с `{"contract":"0x…"}` для статического хостинга. **Быстрая проверка локально:** откройте `creator.html` один раз как `…/creator.html?contract=0xYourPolygonAddress` — UI сохраняет значение в **sessionStorage** для этой вкладки (ключ зависит от chain id), так что вы сможете убрать query и продолжить в рамках той же сессии.

---

## Текущий релиз

| | |
|:--|:--|
| **Рекомендуемая PoC-линия** | **v0.2** — см. **[`RELEASE_v0.2.md`](RELEASE_v0.2.md)** для удобного обзора. Сейчас это **самый стабильный proof-of-concept** в репозитории (gas-only правила контракта, опциональный `dataUrl`, обновленный UI). |
| **Текущий mainnet деплой v0.2** | Polygon PoS — [`0x6c83c8C2e18c183a2776431a23187832b42FfFBb`](https://polygonscan.com/address/0x6c83c8C2e18c183a2776431a23187832b42FfFBb) |
| **Исторический деплой v0.1** | Polygon PoS — [`0x380092fA9C708BF01a552247909CF5DeceFb469E`](https://polygonscan.com/address/0x380092fA9C708BF01a552247909CF5DeceFb469E) (on-chain байт **0**). **Не поддерживается** этим референсным UI; указано для прозрачности. Для новых работ используйте **v0.2+**. |
| **Git / tags** | Не все итерации идеи “0.1” были зафиксированы единым аккуратным тегом, пока репозиторий “учился” GitHub workflows — **v0.2** — согласованный snapshot, на который мы ссылаемся. Другие теги могут существовать для истории; ориентируйтесь на **`RELEASE_v0.2.md`** + `main` для текущего PoC-стори. |

После каждого нового mainnet-деплоя **v0.2** обновляйте релизный тег и заменяйте адрес в трёх полях `NET.contract` в `web/`.

---

**Открытый стандарт аутентификации физических и цифровых объектов через блокчейн.**

Без платформы. Без подписки. Без центрального органа.

Любой может это реализовать. Любой может проверять. Навсегда.

---

## Этот репозиторий (рамки)

То, что вы видите здесь, — это **один пример**, как протокол может быть реализован; это **не** единственно возможный продукт. **`SPEC.md`** — источник истины для совместимых реализаций; этот README — обзор.

---

## Что это такое

ODP позволяет зарегистрировать любой физический или цифровой объект в блокчейне Polygon и подтвердить его подлинность — используя человекочитаемый ID, криптографический хэш и (для физических объектов) физическую привязку (seal).

```
ODP-2026-03-004829301   ← Passport ID (на этикетке, упаковке, сайте)
C-482-930-174-005      ← ID профиля (ваша постоянная идентичность)
```

Любой, у кого есть телефон, может отсканировать QR и проверить:

- кто это сделал
- когда это было зарегистрировано
- менялись ли данные (и если да — обнаружит ли это протокол)
- сохранен ли физический seal (в зависимости от выбранного метода seal)

---

## Как это работает

```
1. Регистрируете профиль    (~US$0.02 типично, один раз — gas + fee)
       ↓
2. Регистрируете ваш объект        (~US$0.02 типично — gas + fee)
   → генерируется Passport ID
   → хэш данных сохраняется в on-chain
       ↓
3. Печатаете метку верификации
   → QR code  odp://ODP-2026-03-004829301
   → Passport ID
   → ID профиля
   → физический seal (см. ниже)
       ↓
4. Любой сканирует → верификатор проверяет блокчейн → подлинность ✓
```

---

## Структура репозитория

```
/
├── SPEC.md                    ← спецификация протокола (English)
├── LICENSE                    ← MIT (copyright: Andrei Chernikov)
├── CONTRIBUTING.md            ← как вносить вклад; labels & PR flow
├── CODE_OF_CONDUCT.md         ← Contributor Covenant
├── docs/
│   ├── README.md                   ← оглавление docs + ссылка на `.odp`
│   ├── VERSIONING_AND_RELEASES.md  ← теги, main, заморозка v0.1, hotfix'ы
│   ├── V0.2-DRAFT.md               ← исторические заметки ранней линии v0.2
│   └── V0.3-DRAFT.md               ← рабочий черновик backlog для потенциальной линии v0.3
├── .github/
│   ├── workflows/
│   │   └── pages.yml          ← GitHub Pages: deploy /web (включите “GitHub Actions” в Settings → Pages)
│   ├── ISSUE_TEMPLATE/        ← Bug, feature, обсуждение спецификации (+ config ссылки контактов)
│   ├── pull_request_template.md
│   ├── BRANCH_PROTECTION.md   ← опциональная защита веток (можно включить позже в Settings)
│   └── profile/
│       ├── README.md          ← текст для landing-страницы организации (скопируйте в org repo `object-digital-passport/.github`)
│       └── PUBLISH.md         ← как опубликовать этот README в GitHub
│
├── contracts/
│   └── ObjectDigitalPassport.sol   ← Solidity smart contract
│
├── deploy/
│   ├── package.json
│   ├── hardhat.config.js
│   └── scripts/
│       └── deploy.js
│
├── tools/
│   └── mint.py                ← CLI mint; пишет `passports/<Passport ID>.odp` (как веб)
│
└── web/
    ├── .nojekyll              ← чтобы статическая загрузка не обрабатывалась как Jekyll (когда нужно)
    ├── creator.html           ← регистрация профиля (пример UI)
    ├── passport.html          ← минт паспортов (пример UI)
    └── verify.html            ← проверка паспортов (пример UI)
```

---

## Быстрый старт (пример веб-интерфейса)

Следующие шаги используют **live demo** ссылки выше — **рабочие примеры** на официальном контракте линии **v0.2**.

### 1. Кошелёк

- **Десктопный браузер:** любое расширение **EIP-1193**, которое инжектит **`window.ethereum`**, может подключиться (см. “Кошельки (в браузере)” вверху этого README). **MetaMask** — популярный вариант; **Rabby**, **Coinbase Wallet**, **Brave Wallet** и другие тоже работают так же, когда вы нажимаете **Connect**.
- **Телефон:** установите приложение кошелька (**MetaMask** или другое, которое поддерживает Polygon и in-app browsing). Откройте демо в **in-app browser** кошелька, чтобы страница получила injected provider — та же идея для десктопа.

Создайте кошелёк, сохраните seed-фразу (backup) и добавьте **Polygon PoS**, если вас попросят.

### 2. POL для gas и комиссий

Регистрация профиля и минт паспорта происходят через **транзакции** в Polygon PoS. Вы платите только **network gas** в **POL**. В линии v0.2 отдельной протокольной комиссии 0.001 POL нет. Страница **Verify** работает в режиме read-only и **не требует** POL или кошелёк. Обновление URL on-chain — также только **gas**.

### 3. Официальный контракт

Таблица **Текущий релиз** показывает канонический mainnet-деплой линии v0.2.

### 3.1 Требования к компиляции (важно)

Для этого контракта включайте оптимизатор и IR-пайплайн:

- `optimizer.enabled = true` (рекомендуется `runs = 200`)
- `viaIR = true`

Без `viaIR` в некоторых окружениях возникает `Stack too deep` в `mintPhysical`.

Важно: `solc` CLI не имеет единого, надёжного флага `--via-ir` во всех версиях.
Если вы компилируете через CLI, используйте Remix “viaIR” или компилируйте через standard JSON с `viaIR: true`
(это в репозитории уже описано в операторских заметках).

Примечание по lifecycle: в контракте есть **одноразовое необратимое** действие `freeze()`, доступное deployer-адресу.  
Оно не меняет существующие записи и правила — только останавливает новые записи.

**Хостинг папкой (`passport.html`):** имя файла на вашем сервере должно строго совпадать с форматом **`<Passport ID>.json`** (та же строка, что поле `humanId`; например `ODP-2026-03-004829301.json`), а зарегистрированный `dataUrl` должен быть **полной HTTPS ссылкой** на этот файл (например `https://example.com/passport/ODP-2026-03-004829301.json`). Solidity-контракт в этом репозитории умеет резолвить `folderBase + "/" + humanId + ".json"` **внутри транзакции mint** при пере-деплойменте (`dataUrlIsFolderBase`); текущий публичный Polygon-деплой ещё использует более старый ABI, поэтому веб-UI держит **`NET.supportsFolderBaseMint: false`** и делает **вторую** транзакцию (`updatePassportUrls`), чтобы заменить временный mint-URL. Поставьте **`supportsFolderBaseMint: true`** только после деплоймента обновлённого контракта и вставки его адреса.

Если вам нужен **отдельный** деплой (например, приватный тест), используйте stack из `deploy/` и подключайте адреса согласно **`SPEC.md`** — этот workflow предназначен для операторов и интеграторов, и не нужен для попытки публичного демо.

### 4. Регистрируйте профиль

**Пример UI:** откройте **[Профиль (live demo)](https://object-digital-passport.github.io/object-digital-passport/creator.html)** — **Connect Wallet**, затем пройдите шаги регистрации.

При регистрации выберите тип:

- **`C`** — Creator (индивидуальный художник, фотограф, автор)
- **`B`** — Brand (компания, студия, лейбл)
- **`P`** — Proof Institution (экспертиза, сертификация, аттестации аукционных домов)
- **`M`** — Museum / collection (для институциональных holdings; музеям использовать `M`, не `B`)

Вы получите постоянный ID вида `C-482-930-174-005`.

**Публикуйте ваш ID публично** — на вашем сайте, в соцсетях и на физических объектах. Именно так другие подтверждают, что паспорт был выдан вами.

```
Short:  C-482-930-174-005
Full:   C-482-930-174-005 / Your Name / 0x742d35Cc...
```

### 5. Минтите паспорт

**Пример UI:** откройте **[Passport (live demo)](https://object-digital-passport.github.io/object-digital-passport/passport.html)**, подключите кошелёк, заполните форму и нажмите **Mint Passport**. После mint'инга вы получаете **`<Passport ID>.odp`** (ZIP по **SPEC §15**): внутри — **`passport.json`** для публикации по **`dataUrl`**, при необходимости **`original/*`** / **`image/*`**, плюс **`manifest.json`** (только для UX, не якорь доверия). Распакуйте архив и загрузите те же байты `passport.json`, что в бандле.

*(CLI: **`tools/mint.py`** после успешного минта сохраняет такой же **`.odp`** в `passports/` — не обязательно для демо в браузере.)*

#### Хостинг `passport.json` на сторонних сайтах

Верификатор загружает ваш `dataUrl` в браузере. URL должен:

- быть **HTTPS**
- возвращать **сырой JSON** — HTTP-ответ должен содержать только байты паспорта (не HTML GitHub-страницы, не login wall)
- для GitHub/Git-Forge использовать **Raw** ссылку (`raw.githubusercontent.com/...`), а не blob-viewer
- поддерживать **CORS** — хост должен разрешать cross-origin `GET` для страницы верификации (у многих статических хостингов и GitHub Raw это разрешено)
- загружать **неизменные байты** — файл нужно отправлять без правок с того скачивания, которое даёт mint

Если вам нужен Passport ID в пути URL, минтите с “стабильным” URL сначала, загрузите файл, затем используйте **Update hosting URLs** в UI кошелька (gas только — без второго mint fee).

Полное нормативное описание: **SPEC.md §9 — Hosting `dataUrl` (third-party sites)**.

### 6. Проверка

**Пример UI:** откройте **[Verify (live demo)](https://object-digital-passport.github.io/object-digital-passport/verify.html)** в любом браузере. Введите Passport ID или вставьте URI в формате `odp://`. **Кошелёк не нужен.**
Проверяйте офлайн: загрузите в верификатор **`.odp` бандл** — он сверяет встроенный `passport.json` с on-chain `dataHash` и (если применимо) проверяет байты оригинала/изображения по on-chain `fileHash` / `imageHash`.

**Шаблон прямой ссылки:**

```
https://object-digital-passport.github.io/object-digital-passport/verify.html?id=ODP-2026-03-004829301
```

---

## Физическая привязка (Physical seal)

Для физических объектов seal обязательна: она связывает цифровой паспорт с **конкретным** объектом перед вами.

### Нумерованный seal (простой, широко используемый)

Подходит любая seal, где есть доказательство вскрытия и **уникальный напечатанный номер** — голографическая наклейка, восковая печать, пломба, нумерованная этикетка. Запишите номер и описание в паспорте; любой сможет сверить физическую метку с документом.

- Оценочная стоимость: от ~$0.05 за штуку (зависит от материала)
- Легко найти в разных масштабах

Этот референсный демонстрационный пример сделан вокруг понятных рабочих процессов; **нумерованные seal** закрывают большинство потребностей мастеров без дополнительного оборудования.

### NFC криптосил (высокая надёжность)

Протокол поддерживает опциональную **криптографическую NFC seal** (challenge–response с ключом, привязанным к чипу). Требования строгие (см. **`SPEC.md` §6**): “обычные” NFC стикеры не подходят для этого профиля. Выбор продукта и UX верификации — детали реализации; следуйте спецификации, если реализуете этот путь.

### Метка верификации

Метка верификации опциональна и в первую очередь повышает удобство для конечного пользователя. Базовое доверие строится на on-chain записи и состоянии seal (NFC / нумерованная пломба).

Если вы используете метку, добавьте:

- QR code (`odp://ODP-YYYY-MM-NNNNNNNNN`)
- Passport ID текстом
- метку протокола (`ODP`)

ID профиля на метке — опционален и только справочный. Для доверия используйте ID профиля из верифицированных публичных источников (официальный сайт / публичные профили в соцсетях), затем сверяйте его с проверкой on-chain.

Если вы используете метку, она должна физически покрывать/удерживать seal так, чтобы при снятии метки seal была повреждена или хотя бы нарушена (см. **`SPEC.md`** правила удержания seal).

---

## Цифровые объекты

Для цифрового искусства, видео, 3D-моделей и других цифровых файлов:

1. **Регистрируйте до публикации** исходный файл
2. On-chain сохраняется SHA-256 хэш исходного файла
3. Тот, кто первым зарегистрировал хэш, и является “автором” в записи
4. После регистрации публикуйте только производные версии (сжатые, watermarked, уменьшенное разрешение)
5. Оригинальный файл остаётся вашей криптографической “пруф”-проверкой авторства

C2PA совместимость: если ваш файл содержит встроенный C2PA манифест
(из Photoshop, Lightroom, Leica camera и т.п.), то хэш файла автоматически включает и манифест. Никаких дополнительных шагов не нужно.

Опциональная явная C2PA-проверка: укажите `digital.c2pa.manifestHash` (`sha256:...`) в `passport.json`.
В текущем MVP UI этот хэш вводится из внешнего C2PA-инструмента и сверяется в Verify (ручное сравнение хэша).

---

## Proof Institution

Музеи, галереи и эксперты могут регистрироваться как тип `P`
и прикреплять attestations к любому паспорту:

```
ODP-2026-03-004829301
  └── Proof от P-029-384-751-224  Garage Museum    2031
  └── Proof от P-774-002-391-888  Sotheby's        2051
```

Регистрация открыта — утверждение не требуется.
Институции обязаны публиковать свой ID публично, чтобы любой мог проверить их идентичность.

Опциональная **P-аффилиация** связывает два ID профиля типа `P` в одноуровневых отношениях parent–child:
child `P` предлагает связь, parent `P` подтверждает on-chain; у каждого child не более одного parent `P`.
Реальный смысл связи (головной офис и подразделение, сеть и участник и т.д.) задаётся оффчейн;
протокол обеспечивает антиспам-модель с подтверждением двумя сторонами.

---

## SDK

Чтобы собрать верификатор или интегрировать ODP в приложение:

```javascript
// Минимальная верификация — чтение из блокчейна
const provider = new ethers.providers.JsonRpcProvider("https://polygon-rpc.com");
const contract = new ethers.Contract(CONTRACT_ADDRESS, ABI, provider);

const record   = await contract.getPassport("ODP-2026-03-004829301");
const creator  = await contract.getCreator(record.creatorId);
const proofIds = await contract.getProofsForPassport("ODP-2026-03-004829301");
```

Используйте **адрес контракта из Current release** для `CONTRACT_ADDRESS` в Polygon PoS. Чтение бесплатно. Кошелёк или API key не нужны.

См. `SPEC.md` секцию 13 для полного описания интерфейса SDK.

---

## Сеть

| | |
|:--|:--|
| Сеть | Polygon PoS |
| Chain ID | 137 |
| Типичный cost при mint/register (только gas, v0.2) | ~US$0.01 (зависит) |
| Testnet | Polygon Amoy (chain id 80002) |
| Исторический контракт (v0.1) | [`0x3800…469E`](https://polygonscan.com/address/0x380092fA9C708BF01a552247909CF5DeceFb469E) — не поддерживается этим UI. |
| Контракт (текущий v0.2) | [`0x6c83c8C2e18c183a2776431a23187832b42FfFBb`](https://polygonscan.com/address/0x6c83c8C2e18c183a2776431a23187832b42FfFBb) |

---

## Стоимость

**v0.2+** (упакованный байт on-chain **≥ 2**): транзакции **register**, **mint** и **proof** платят **только gas** сети Polygon (POL) — **без** отдельного протокольного fee в этой строке.

Старые деплойменты **v0.1** использовали отдельное сжигаемое fee на некоторых действиях; этот референсный UI не нацелен на такие контракты.

| Действие | Оценочная стоимость (v0.2) |
|:--|:--|
| Регистрация профиля | ~US$0.01 (разово) — gas only |
| Mint a passport | ~US$0.01 — gas only |
| Submit a proof | ~US$0.01 — gas only |
| Обновлять только URL паспорта (`updatePassportUrls`) | Только gas |
| Проверить объект | Бесплатно |
| Читать любые данные | Бесплатно |

---

## Спецификация

Полная спецификация протокола — в [`SPEC.md`](SPEC.md).
Она описывает ровно то, как генерируются ID, как вычисляются хэши,
как работает верификация и что должен реализовать любой SDK.

**Оглавление документов:** [`docs/README.md`](docs/README.md). Неформальные / черновые заметки: [`docs/V0.2-DRAFT.md`](docs/V0.2-DRAFT.md), [`docs/V0.3-DRAFT.md`](docs/V0.3-DRAFT.md) (RU: [`localization/ru/V0.3-DRAFT.md`](localization/ru/V0.3-DRAFT.md)).

Любой разработчик может построить совместимую реализацию,
только по спецификации — не читая код этого репозитория.

Для threat model, известных рисков и рекомендаций по безопасности:
см. [`SECURITY.md`](SECURITY.md).

---

## Дорожная карта

| | |
|:--|:--|
| **Референсный старт (v0.1)** | *Сейчас* — черновая спецификация, примерный стек в этом репозитории, контракт и веб-UI как рабочая иллюстрация. |
| **Промежуточные вехи** | *TBD* — обратная связь экосистемы, итерации testnet/mainnet, уточнения спецификации (отслеживаются через issues и PR). |
| **Стабильный релиз v1.0** | **январь 2027** — цель для стабильной версии протокола и совместимых инструментов **v1** (см. правила версионирования SPEC). |

До v1.0 ожидаются ломающие изменения в черновой спецификации (0.x). Реализациям стоит “прибить” (pin) версию спецификации, которую они поддерживают.

---

## Вклад (Contributing)

Это черновая спецификация (v0.1). Цель — получить обратную связь.

- **[`CONTRIBUTING.md`](CONTRIBUTING.md)** — fork/PR flow, labels, рамки
- **[`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)** — Contributor Covenant (ожидания сообщества)
- **[`docs/README.md`](docs/README.md)** — что лежит в `docs/` и где описан `.odp`
- **[`docs/VERSIONING_AND_RELEASES.md`](docs/VERSIONING_AND_RELEASES.md)** — теги vs `main`, заморозка линии (например v0.1), hotfix tags
- **[`.github/BRANCH_PROTECTION.md`](.github/BRANCH_PROTECTION.md)** — опциональные правила защиты веток (включайте на GitHub, когда будете готовы; в ежедневной работе не обязательно)
- **[`.github/profile/README.md`](.github/profile/README.md)** — текст для главной страницы **организации**; публикуется отдельным org repo **`.github`** — см. **[`.github/profile/PUBLISH.md`](.github/profile/PUBLISH.md)**

Быстрые подсказки:

- Открывайте issue для обсуждения изменений в спецификации
- PR приветствуются для контракта, туллинга и документации
- Чтобы предложить новый префикс типа эмитента (C/B/P/M) — сначала откройте обсуждение в спецификации

---

## Автор

**Object Digital Passport** (референсная реализация, спецификация, контракт и веб-UI) создан **Andrei Chernikov**.

---

## Лицензия

[MIT](LICENSE) — используйте свободно, стройте поверх, форкайте.

---

*Object Digital Passport — open source.*
*Spec v0.1 — черновик; стабильная **v1.0** запланирована на январь 2027 (см. раздел «Дорожная карта»).*

