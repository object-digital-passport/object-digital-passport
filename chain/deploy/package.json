{
  "name": "object-digital-passport-deploy",
  "version": "0.1.0",
  "author": "Andrei Chernikov",
  "description": "Deploy scripts — Hardhat runs from chain/ (../package.json).",
  "type": "module",
  "scripts": {
    "compile": "cd .. && npm run compile",
    "test": "cd .. && npm run test",
    "deploy:testnet": "cd .. && npm run deploy:testnet",
    "deploy:mainnet": "cd .. && npm run deploy:mainnet"
  }
}
