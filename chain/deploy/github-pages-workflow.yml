# DEPRECATED TEMPLATE — do not copy.
# The live workflow is maintained at .github/workflows/pages.yml (site split:
# landing / + rendered /spec/ + reference UI /demo/ + legacy redirects).
# This file is kept only so old links do not 404; it will be removed in a
# future cleanup.
